#!/usr/bin/python3

import sys
from os.path import abspath, dirname, join

try:
    import tincapplet
except ImportError:
    sys.path.append(abspath(join(dirname(__file__), '..')))

from tincapplet import tincindicator
tincindicator.main()
